# Required for claude-hype
ANTHROPIC_API_KEY=sk-ant-...

# Required for claude-history semantic search (optional — keyword search works without it)
OPENAI_API_KEY=sk-...

# Optional: override the default user name in claude-hype (defaults to OS username)
# CLAUDE_HYPE_USER=YourName
