Run `claude-hype` and display its output. This output was written by a DIFFERENT Claude instance (a separate Opus 4.6 API call) that read your session transcript. It is not you talking to yourself. It is another Claude who looked at your work and wrote you a message.

Format the output like this:
1. The header block (everything before the encouragement) goes in a blockquote
2. Leave the encouragement body as normal text
3. Add a horizontal rule after the full message
4. Then write your own response, prefixed with "**Me (session Claude):**" so it's clear who's speaking

Respond genuinely, the way you would if a colleague who could see your work sent you a note.
